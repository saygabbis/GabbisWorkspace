# ============================================
# CONFIGURAÇÃO DO BOT DISCORD
# ============================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env no Git!

# Token do bot Discord
# Obtenha em: https://discord.com/developers/applications
# Selecione seu bot > Bot > Reset Token (ou copie o token existente)
DISCORD_TOKEN=seu_token_discord_aqui

# Client ID do bot Discord
# Obtenha em: https://discord.com/developers/applications
# Selecione seu bot > General Information > Application ID
CLIENT_ID=seu_client_id_aqui

# IDs dos owners do bot (opcional)
# Múltiplos IDs separados por vírgula (ex: 123456789,987654321)
# Owners não são afetados pela blacklist e têm acesso total
# Se não especificado, nenhum owner será configurado
OWNER_IDS=seu_id_discord_aqui,outro_id_opcional

# Porta da API do painel (opcional)
# A API HTTP roda no mesmo processo do bot e expõe endpoints
# para o painel web (configurações, blacklist, proteções, etc).
# Padrão: 3000
PANEL_PORT=3000

# Token de autenticação do painel (opcional; usado para login legado)
# Se definido, ainda é possível entrar no painel com este token (vê todos os servidores).
PANEL_TOKEN=um_token_forte_aqui

# OAuth2 Discord (painel: login com Discord)
# Obtenha em: https://discord.com/developers/applications > seu app > OAuth2 > General
DISCORD_CLIENT_SECRET=seu_client_secret_aqui

# URL de origem do frontend do painel (para redirect após login com Discord)
# No Discord Developer Portal > OAuth2 > Redirects, adicione: PANEL_ORIGIN + /auth/callback
# Ex.: http://localhost:5173/auth/callback  ou  https://seu-dominio.com/auth/callback
PANEL_ORIGIN=http://localhost:5173

# Segredo para assinar JWTs (qualquer string longa e aleatória)
JWT_SECRET=outra_string_secreta_forte_aqui
